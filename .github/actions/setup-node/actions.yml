name: "Setup Node"

defaults:
  run:
    shell: bash

runs:
  using: "composite"
  steps:
    - name: Setup node
      uses: actions/setup-node@v4
      with:
        node-version-file: '.node-version'
